apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-api-deployment
  {{- with .Values.deployment }}
  labels:
    {{- range $key,$value := .labels }}
    {{ $key }}: {{ $value }}
    {{- end }}
spec:
  selector:
    matchLabels:
      {{- range $key,$value := .selector.matchLabels }}
      {{ $key }}: {{ $value }}
      {{- end }}
  template:
    metadata:
      labels:
        {{- range $key,$value := .template.labels }}
        {{ $key }}: {{ $value }}
        {{- end }}
    spec:
      containers:
<<<<<<< HEAD
        {{- $imageVersion := .imageVersion }}
        {{- range .containers }}
        - name: {{ .name }}
          image: {{ .image }}: {{ $imageVersion }}
=======
        - name: inventory-api-container
          image: docker.io/macsi21/inventory-api:v1
          readinessProbe:
            httpGet:
              path: "/hc"
              port: 80
            initialDelaySeconds: 10
            timeoutSeconds: 5
>>>>>>> parent of 93b7637... Change image version for api deployment
          ports:
            {{- range .ports }}
            - containerPort: {{ .containerPort }}
            {{- end }}
          env:
            {{- range $.Values.deployment.env }}
            - name: {{ .name }}
              {{- if .value }}
              value: {{ .value }}
              {{- end }}
              {{- if .valueFrom }}
                valueFrom:
                  {{- if .configMapKeyRef }}
                  configMapKeyRef:
                    {{- range $key,$value := .configMapKeyRef }}
                    {{ $key }}: {{ $value }}
                    {{- end}}
                  {{- end }}
                  {{- if .secretKeyRef }}
                  secretKeyRef:
                    {{- range $key,$value := .secretKeyRef }}
                    {{ $key }}: {{ $value }}
                    {{- end}}
                  {{- end }}  
              {{- end }}
          readinessProbe:
            httpGet:
              path: {{ $.Values.deployment.readinessProbe.healthCheck.path }}
              port: {{ $.Values.deployment.readinessProbe.healthCheck.port }}
            initialDelaySeconds: {{ $.Values.deployment.readinessProbe.initialDelaySeconds }}
            timeoutSeconds: {{ $.Values.deployment.readinessProbe.timeoutSeconds }}
        {{- end }}
      {{- if $.Values.rbac.serviceAccount.enabled }}
      serviceAccount: {{ $.Values.rbac.serviceAccount.name }}
      serviceAccountName: {{ $.Values.rbac.serviceAccount.name }}
      {{- end }}
    {{- end }}